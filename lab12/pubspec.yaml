name: lab12
description: "A new Flutter project."
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ^3.10.4

dependencies:
  flutter:
    sdk: flutter

  # สำคัญ: ต้องเว้นวรรคให้ตรงกับคำว่า flutter: ด้านบน (2 เคาะจากขอบซ้าย)
  provider: ^6.1.1
  sqflite: ^2.3.0
  path: ^1.9.0
  cupertino_icons: ^1.0.8

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true