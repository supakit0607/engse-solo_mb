# Lab 6: Flutter Navigation and Routing Demo

โปรเจกต์ Flutter นี้เป็นส่วนหนึ่งของการศึกษาในวิชา **Mobile Devices Application Design and Development** โดยมีวัตถุประสงค์เพื่อสาธิตการใช้งานวิดเจ็ตต่างๆ ของ Material Design และการจัดการระบบนำทาง (Navigation) ใน Flutter

## 🎯 วัตถุประสงค์
* ศึกษาการใช้งาน Material Component Widgets ในหมวดหมู่ต่างๆ
* ฝึกปฏิบัติการจัดการเส้นทางภายในแอปด้วยระบบ **Named Routes**
* เข้าใจการรับ-ส่งข้อมูลและการจัดการสถานะ (State) เบื้องต้นในแต่ละหน้าจอ

---

## 🏗️ โครงสร้างและการทำงานของแอปพลิเคชัน
แอปพลิเคชันเริ่มต้นที่ `HomePage` ซึ่งทำหน้าที่เป็นเมนูหลัก (Main Menu) แสดงรายการหมวดหมู่ของวิดเจ็ตต่างๆ โดยแบ่งออกเป็นหน้าย่อยดังนี้:

### 1. 💬 CommunicationPage
* **วัตถุประสงค์:** สาธิตการใช้วิดเจ็ตเพื่อการสื่อสารข้อมูลกับผู้ใช้
* **วิดเจ็ตที่ใช้:** * `SnackBar`: แสดงข้อความแจ้งเตือนชั่วคราวที่ด้านล่างหน้าจอ
    * `AlertDialog`: แสดงกล่องโต้ตอบเพื่อยืนยันหรือแจ้งข้อมูลสำคัญ

### 2. 📦 ContainmentPage
* **วัตถุประสงค์:** สาธิตการใช้วิดเจ็ตสำหรับจัดกลุ่มหรือครอบเนื้อหา
* **วิดเจ็ตที่ใช้:** * `Card`: แสดงการ์ดที่มีมิติ (Elevation) เพื่อจัดกลุ่มข้อมูลให้ดูเป็นสัดส่วน

### 3. 🚀 NavigationPageDemo
* **วัตถุประสงค์:** สาธิตระบบ Stack Navigation เบื้องต้น
* **การทำงาน:** * ใช้ `Navigator.push()` เพื่อนำทางไปยัง `SecondPage`
    * ใช้ `Navigator.pop()` เพื่อย้อนกลับมายังหน้าก่อนหน้า

### 4. ✅ SelectionPage
* **วัตถุประสงค์:** สาธิตการใช้วิดเจ็ตสำหรับการเลือกตัวเลือกต่างๆ
* **วิดเจ็ตที่ใช้:** * `CheckboxListTile`: เลือกได้หลายตัวเลือก (True/False)
    * `SwitchListTile`: การเปิด/ปิดสถานะ
    * `RadioListTile`: การเลือกตัวเลือกเดียวจากกลุ่ม (เช่น การเลือกเพศ)

### 5. ⌨️ TextInputPage
* **วัตถุประสงค์:** สาธิตการรับข้อมูลประเภทข้อความจากผู้ใช้
* **วิดเจ็ตที่ใช้:** * `TextField`: ช่องกรอกข้อมูล
    * `ElevatedButton`: ปุ่มสำหรับ Submit ข้อมูลเพื่อนำมาแสดงผลบนหน้าจอ

---

## 🛣️ ระบบการนำทาง (Navigation and Routing)
โปรเจกต์นี้จัดการเส้นทางด้วยระบบ **Named Routes** เพื่อให้โค้ดสะอาดและจัดการง่ายในโปรเจกต์ขนาดใหญ่:

* **การตั้งค่า:** กำหนดใน `MaterialApp` ผ่านทาง `initialRoute` และ `routes`
* **การเรียกใช้งาน:** ใช้ `Navigator.pushNamed(context, routeName)` เพื่อนำทางไปยังหน้าจอที่กำหนดไว้ในเส้นทาง

---

## 🛠️ วิธีการติดตั้งและรันโปรเจกต์
1. Clone โปรเจกต์นี้ไปยังเครื่องของคุณ
2. รันคำสั่ง `flutter pub get` เพื่อติดตั้ง dependencies
3. รันโปรเจกต์ด้วยคำสั่ง `flutter run`

---
**จัดทำโดย:** [นายศุภกิจ รักบุตร]  
**วิชา:** Mobile Devices Application Design and Development